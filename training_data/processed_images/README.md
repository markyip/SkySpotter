# Processed training images (auto-generated)

Training runs **background removal and subject cropping** automatically from `training_data/classified_images/` and writes PNGs here (one subfolder per class).

You do not need to run a separate preprocessing script. Re-run training to refresh images when you add or change source photos.

This folder is ignored by git except this README.
